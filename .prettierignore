# Prettier is only used for markdown, CSS, and SCSS
# Biome handles JavaScript, TypeScript, and JSON formatting

# Dependencies
node_modules/

# Build outputs
.next/
out/
build/
coverage/

# Generated files
package-lock.json
pnpm-lock.yaml
yarn.lock
*.tsbuildinfo
next-env.d.ts

# The completed is-a.dev pull request template. Their validator requires the
# template to be unmodified, and Prettier reflows it — which is exactly the
# kind of change that fails their check.
deploy/is-a-dev/PR-BODY.md
